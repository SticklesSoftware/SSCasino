@{
    //========================================================================================================================
    // WEBSITE: Poker Playground - Experimental virtual poker
    //
    // _ViewStart.cshtml
    //      This file is used to define common view code that will execute at the start of each View’s rendering. It is used
    //      here to set the Layout property for each View to be the _SiteLayout.cshtml file by default, This file defines the
    //      layout framework of the site.
    //
    // Developer Notes
    //      If Areas are in use, some areas in the site use a separate layout file. If so, there will be a _ViewStart.cshtml 
    //      loacted in the Views folder of the area that will point to the layout file.
    //========================================================================================================================
    Layout = "~/Views/Shared/_SiteLayout.cshtml";
}